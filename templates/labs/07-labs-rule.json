{
    "target":["globals","variables","defines"],
    "description": "Numeric characters are not allowed on variable names",
    "criterion":{
        "target":"regex",
        "value": "^[A-Za-z_]+$"
    }

}